import styled from "styled-components";

export const FavoriteScreenStyled = styled.main`
    textAlign: "center"; 
`;

export const FavoriteScreenTitle  = styled.h2`
    fontSize: 64;
`;

export const FavoriteScreenText  = styled.p`
`;