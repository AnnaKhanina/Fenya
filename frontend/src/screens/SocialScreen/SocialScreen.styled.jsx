import styled from "styled-components";

export const SocialScreenStyled = styled.main`
    textAlign: "center"; 
`;

export const SocialScreenTitle  = styled.h2`
    fontSize: 64;
`;

export const SocialText  = styled.pSocial